# docker/env/.env.postgres.example
# Copy to `.env.postgres` (git-ignored) before running `docker compose up`.
# These values must stay in sync with DATABASE_URL in the root .env.
POSTGRES_DB=requirementshub
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
